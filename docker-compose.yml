services:
  app:
    build: .
    image: pho
    ports:
      - "8931:8931"   # admin UI
      - "8932:8932"   # mock-serving surface
    environment:
      - ConnectionStrings__Pho=Data Source=/data/pho.db
    volumes:
      - pho-data:/data
    restart: unless-stopped

volumes:
  pho-data:
