<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <title>Pho — Mock Web Service</title>
    <style>
        body { font-family: system-ui, sans-serif; margin: 0; color: #1a1a1a; background: #fff; }
        main { max-width: 960px; margin: 0 auto; padding: 1.5rem; }
        header h1 { font-size: 1.4rem; }
        table { width: 100%; border-collapse: collapse; margin-top: 1rem; }
        th, td { text-align: left; padding: 0.4rem 0.6rem; border-bottom: 1px solid #e5e5e5; }
        button { margin-right: 0.35rem; }
        label { display: block; margin: 0.6rem 0; }
        input[type=text], input[type=number], textarea, select { width: 100%; max-width: 32rem; padding: 0.3rem; }
        textarea { min-height: 8rem; font-family: monospace; }
    </style>
    <HeadOutlet />
</head>
<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
</body>
</html>
